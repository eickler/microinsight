replicaCount: 1

image:
  repository: ghcr.io/eickler/microinsight
  tag: latest
  pullSecrets: regcred # Note: This value is currently also used in the code.

db:
  host: "mycluster"
  user: "mysql"
  pass: "mysql"
  name: "mydb"

interval: 30
loglevel: "INFO"
